import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#f0f0f0",
    alignItems: "center",
    justifyContent: "center",
  },
  wrapper: {
    backgroundColor: "#fff",
    flexDirection: "row",
    flexWrap: "wrap",
    width: 300,
    marginBottom: 20, //for debugging
  },
});
