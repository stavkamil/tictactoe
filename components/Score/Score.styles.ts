import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  text: {
    fontSize: 30,
    marginBottom: 15,
  },
  button: {
    position: "absolute",
    top: 80,
    backgroundColor: "#000",
    padding: 15,
    borderRadius: 30,
    marginBottom: 15,
  },
  buttonText: {
    color: "#fff",
    fontSize: 20,
    fontWeight: "bold",
  },
});
